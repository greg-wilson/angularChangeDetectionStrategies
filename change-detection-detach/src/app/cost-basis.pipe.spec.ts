import { CostBasisPipe } from './cost-basis.pipe';

describe('CostBasisPipe', () => {
  it('create an instance', () => {
    const pipe = new CostBasisPipe();
    expect(pipe).toBeTruthy();
  });
});
